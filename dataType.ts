// ! Basic data type

// ! number
const num: number = 555;

// ! string
const str: string = "Mohammad";

// ! boolean
const bool: boolean = false;

// ! undefined
const und: undefined = undefined;

// ! null
const n: null = null;

let friends: string[] = ["Mohammad", "Ashif"];
let rollNumber: number[] = [1, 2, 3, 4, 5];


// tuple

let coordinates:[number, number] = [44, 56];
let nameAgeMarried:[string, number, boolean] = ["Biden", 87, true];